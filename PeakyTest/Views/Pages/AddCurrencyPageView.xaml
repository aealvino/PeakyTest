<Page x:Class="PeakyTestUI.Views.Pages.AddCurrencyPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PeakyTestUI.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddCurrencyPageView">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Добавить валюту" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Код" VerticalAlignment="Center" Margin="0,0,12,12"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CharCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Padding="8"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Цифровой код" VerticalAlignment="Center" Margin="0,0,12,12"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NumCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Padding="8"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Валюта" VerticalAlignment="Center" Margin="0,0,12,12"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Padding="8"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Номинал" VerticalAlignment="Center" Margin="0,0,12,12"/>
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Nominal, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Padding="8"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Курс" VerticalAlignment="Center" Margin="0,0,12,12"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Padding="8"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Вчера" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Previous, UpdateSourceTrigger=PropertyChanged}" Padding="8"/>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,12,0,0"/>
        <TextBlock Grid.Row="3" Text="{Binding SuccessMessage}" Foreground="Green" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Отмена" Command="{Binding CancelCommand}" Margin="0,0,8,0" Padding="12,6"/>
            <Button Content="Добавить" Command="{Binding AddCommand}" Padding="12,6"/>
        </StackPanel>
    </Grid>
</Page>